@model IEnumerable<DPSapp.Models.Tag>
<style>
    .button {
        display: inline-block;
        border-radius: 4px;
        background-color: rgb(138 171 204);
        border: none;
        color: #FFFFFF;
        text-align: center;
        font-size: 28px;
        padding: 20px;
        width: 400px;
        transition: all 0.5s;
        cursor: pointer;
        margin: 5px;
    }

        .button span {
            cursor: pointer;
            display: inline-block;
            position: relative;
            transition: 0.5s;
        }

            .button span:after {
                content: '\00bb';
                position: absolute;
                opacity: 0;
                top: 0;
                right: -20px;
                transition: 0.5s;
            }

        .button:hover span {
            padding-right: 25px;
        }

            .button:hover span:after {
                opacity: 1;
                right: 0;
            }
</style>
@{
    ViewBag.Title = "Tag";
}
<div class="jumbotron jumbotron-fluid" style="background-image: linear-gradient(rgb(104 156 207), white)">
    <div class="container" style="  background-image: linear-gradient(rgb(104 156 207), white)">

        <h1 class="display-4" style="text-align:center ;font-family:Broadway">Lista znaczników</h1>


    </div>
</div>

<p id="dodaj" class="button">
    @Html.ActionLink("Utwórz nowy znacznik", "Create")
</p>
<br />

<div class="container">
    <h4 class="display-4" style="text-align:left ;font-family:'Times New Roman'">Wyszukaj znacznik</h4>
    <div class="form-group">
        <div class="input-group">
            <span class="input-group-addon"><i class="glyphicon glyphicon-search"></i> </span>
            <input type="text" id="Search" placeholder="Search" class="form-control" />
        </div>
    </div>
</div>


<h1 class="display-4" style="text-align:center ;font-family:'Times New Roman'">Znaczniki</h1>

<br />
<table class="table table-hover" id="tabelka">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.RecipeName)*@
            @Html.ActionLink("Numer id tagu", "Index", new { sortOrder = ViewBag.IdSortParm })
        </th>
        <th>
            @* @Html.DisplayNameFor(model => model.Date)*@
            @Html.ActionLink("Nazwa", "Index", new { sortOrder = ViewBag.NameSortParm })
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="Search">
            <td>
                @*<a href="=#" data-toggle="popover" data-trigger="hover" id="@item.RecipeID">
                    @item.RecipeName*@
                @Html.DisplayFor(modelItem => item.TagId)
                @*</a>*@


            </td>
            <td>

                @Html.DisplayFor(modelItem => item.TagName)
            </td>
            @*<td>
                    <div>
                        <input type="button" value="Dodaj składnik" onclick="@("window.location.href='" + @Url.Action("Create","Quantity") + "'");" />

                    </div>
                </td>*@

            <td>
                @Html.ActionLink("Edytuj", "Edit", new { id = item.TagId })
            </td>
        </tr>
    }

</table>



