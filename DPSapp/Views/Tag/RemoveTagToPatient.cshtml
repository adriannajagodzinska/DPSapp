<style>
    #tlo {
        background-color: whitesmoke;
    }

    .my-class {
        color: black;
        font-family: Marker Felt;
    }

        .my-class:active {
            color: black;
        }

        .my-class:link {
            color: black;
        }

        .my-class:visited {
            color: black;
        }

        .my-class:hover {
            color: cadetblue;
        }
</style>
@model DPSapp.Models.AddTagToPatientHelper
@{
    ViewBag.Title = "RemoveTagToPatient";
    Layout = "~/Views/Shared/_LayoutEmp.cshtml";
}

<h2>Usuń znacznik od pacjenta</h2>
@using (Html.BeginForm("RemoveTagToPatient", "Tag", FormMethod.Post, new { enctype = "multipart/form-data" }))
{




    <div class="form-horizontal">
        @* <h4>Składnik</h4>*@
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @*@Html.LabelFor(model => model.Tag, "Nazwa znacznika", htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">

                @*@Html.DropDownList("TagName", null, htmlAttributes: new { @class = "form-control" })*@
                @*@Html.DropDownListFor(m => m.TagIdToAdd, Model.ListOfTags, new { @class = "form-control" })*@
                @Html.DropDownListFor(m => m.TagIdToAdd, new SelectList(Model.ListOfTags, "TagId", "TagName"))
                @*@ViewBag.TagID as SelectList*@

                @*@Html.ValidationMessageFor(model => model.TagId, "", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Usuń Znacznik" class="btn btn-default" />
            </div>
        </div>
    </div>

}
<div>
    @Html.ActionLink("Powrót do listy ogłoszeń", "SendedMessages", "Employee", null, new { @class = "my-class", title = "Naciśnięcie spowoduje powrót do listy ogłoszeń" })
</div>