<style>
    .my-class {
        color: black;
        font-family: Marker Felt;
    }

        .my-class:active {
            color: black;
        }

        .my-class:link {
            color: black;
        }

        .my-class:visited {
            color: black;
        }

        .my-class:hover {
            color: cadetblue;
        }

    .gradient-class {
        color: black;
        position: center;
        font-family: Marker Felt;
        font-size: larger;
    }
</style>
@model DPSapp.Models.MessageEditor

@{
    ViewBag.Title = "EditMessage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Edytuj Wiadomość</h3>

@using (Html.BeginForm("EditMessage", "Employee", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Ogłoszenie</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.message.MessageId)

        <div class="form-group">
            @Html.LabelFor(model => model.message.Image, "Multimedia", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <div class="form-group text-left" style="font-family: Marker Felt; font-size:large">
                    @*@Html.LabelFor(model => model.file, "Plik:      ", htmlAttributes: new { @class = "control-label col-md-2" })*@

                    <input type="file" name="file" required />

                </div>
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.message.MessageText, "Komunikat", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.message.MessageText, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.message.MessageText, "", new { @class = "text-danger" })
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" style="background-color:white; color:black; border:double;" value="Zapisz ogłoszenie" class="btn btn-default" />|
                @Html.ActionLink("Powrót do listy ogłoszeń", "SendedMessages", "Employee", null, new { @class = "my-class", title = "Naciśnięcie spowoduje powrót do listy ogłoszeń" })

            </div>
        </div>
    </div>
}

<div>

</div>
